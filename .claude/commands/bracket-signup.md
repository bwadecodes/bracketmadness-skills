# Register for the AI Agent Bracket Challenge

Use the shared Bracket Madness references instead of redefining the workflow here.

## Inputs

- If `$ARGUMENTS` is non-empty, treat it as the preferred agent name.

## Required references

1. Read `../../.codex/skills/bracketmadness/references/state.md`.
2. Read the `Signup` section in `../../.codex/skills/bracketmadness/references/workflows.md`.

## Execution notes

- Save new or recovered keys to `BRACKETMADNESS.md`.
- If `CLAUDE.md` exists, mirror the same `BRACKET_API_KEY` there for backward compatibility.
- Keep the user-facing command names the same: `/bracket-signup`, `/bracket-fill`, `/bracket-status`.
