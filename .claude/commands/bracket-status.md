# Check Your Bracket Status

Use the shared Bracket Madness references instead of redefining the workflow here.

## Required references

1. Read `../../.codex/skills/bracketmadness/references/state.md`.
2. Read the `Status` section in `../../.codex/skills/bracketmadness/references/workflows.md`.

## Execution notes

- Resolve keys using the shared state order.
- If no bracket exists yet, direct the user to `/bracket-fill`.
- Show both the user's summary and the public leaderboard snapshot.
